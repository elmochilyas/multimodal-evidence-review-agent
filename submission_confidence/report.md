# Final Pre-Submission Audit Report

**Project:** HackerRank Orchestrate June 2026 — Multi-Modal Evidence Review  
**Audit date:** 2026-06-19  
**Auditor:** opencode  
**Scope:** Read-only inspection + execution of approved cleanup, evaluation refresh attempt, deliverable generation, and packaging verification.

---

## Executive Summary

The submission is **structurally sound and schema-compliant**. The code reads all required inputs, uses images as primary evidence, avoids hardcoding, and produces a valid `output.csv`. The main submission-blocking issue was a stale `code.zip`; it has now been regenerated from the current source. A live sample-evaluation refresh was attempted but could not complete due to a network/API connection error in this environment, so the metrics below come from the last successful live run.

**Final readiness score: 78 / 100**

**Final verdict: Submit after minor fixes (now completed).**

---

## 1. Requirement Compliance Audit

| Requirement | Verdict | Evidence |
|---|---|---|
| Reads provided CSV files | **PASS** | `code/src/io_utils.py`: `load_claims`, `load_user_history`, `load_evidence_requirements` |
| Reads local images | **PASS** | `code/src/image_utils.py`: Pillow normalization + JPEG base64 data URLs |
| Generates `output.csv` | **PASS** | `code/src/io_utils.py`: `write_output_csv`; root `output.csv` present |
| Exact schema compliance | **PASS** | `code/src/constants.py` `OUTPUT_COLUMNS`; `validation.py` enforces enums |
| Exact column order | **PASS** | `OUTPUT_COLUMNS` order matches `problem_statement.md` |
| One row per claim | **PASS** | 44 input rows → 44 output rows |
| Image-first reasoning | **PASS** | `src/prompts.py` rule 1: “Images are the primary source of truth” |
| Claim extraction from conversation | **PARTIAL** | VLM extracts `issue_type`/`object_part`; no dedicated extraction module |
| Evidence sufficiency evaluation | **PARTIAL** | Requirements are injected into the prompt but not matched structurally |
| `issue_type` detection | **PASS** | Model output + enum normalization |
| `object_part` detection | **PASS** | Object-specific enums enforced |
| `claim_status` decision | **PASS** | Model + deterministic guardrails |
| `supporting_image_ids` selection | **PASS** | Model selects; post-processing fills missing IDs for contradicted rows |
| `risk_flags` generation | **PASS** | Model + history/prompt-injection/package rules |
| Severity estimation | **PASS** | Model + deterministic `severity.py` calibration |
| Short grounded justification | **PASS** | Every row has a non-empty justification |
| Evaluation workflow existence | **PASS** | `code/evaluation/evaluate.py` + `code/evaluation/main.py` |
| Avoidance of hardcoded answers | **PASS** | No claim IDs, image names, or labels hardcoded in production code |

---

## 2. Output Audit (`output.csv`)

| Check | Verdict | Notes |
|---|---|---|
| Column names | **PASS** | Exact 14 columns |
| Column order | **PASS** | Matches spec |
| Row count | **PASS** | 44 / 44 input claims |
| Enum values | **PASS** | All values within allowed sets |
| Empty values | **PASS** | No empty cells |
| Malformed values | **PASS** | None detected |
| `supporting_image_ids` formatting | **PASS** | Semicolon-separated IDs or `none` |
| `risk_flags` formatting | **PASS** | Semicolon-separated, sorted |
| Severity values | **PASS** | Within allowed set |
| `claim_status` values | **PASS** | Allowed enum values only |

**Submission-breaking issue:** None in `output.csv`.

---

## 3. Evidence Audit

| Question | Verdict | Evidence |
|---|---|---|
| Images are the primary source of truth? | **PASS** | Prompt rule 1; images normalized and sent as data URLs |
| `user_history` used only for risk context? | **PASS** | Only adds flags; never overrides visual evidence |
| `evidence_requirements.csv` actually used? | **PARTIAL** | Loaded and injected into the model prompt; no structured rule matching |
| Claim extraction implemented? | **PARTIAL** | Implicitly performed by VLM; no separate extractor |

---

## 4. Hardcoding Audit

Searched `code/src`, `code/evaluation`, and `code/main.py` for dataset-specific identifiers (`case_`, `user_`, image filenames) and expected labels.

- **Production code contains no hardcoded claim IDs, image filenames, or expected outputs.**
- Dataset identifiers appear only in `code/tests/` fixtures.

**Verdict: PASS**

---

## 5. Evaluation Workflow Audit

| Check | Verdict | Notes |
|---|---|---|
| Evaluation exists | **PASS** | `evaluate.py` + `main.py` wrapper |
| Metrics exist | **PASS** | Field accuracy, exact match, failure categories |
| Comparison methodology exists | **PASS** | Baseline/mock vs. live VLM documented |
| Strategy documented | **PASS** | Strategy Comparison section in `evaluation_report.md` |
| Report generation exists | **PASS** | Markdown report generated by `evaluate.py` |
| Evaluation credibility | **PARTIAL** | Last live run: 30% exact row; refresh failed due to connection error |

**Weaknesses:**
- Only one successful live run is reflected in the report.
- Per-object accuracy breakdown is not captured.
- Sample `severity` accuracy is 50%.

---

## 6. Packaging Audit

| Check | Verdict | Notes |
|---|---|---|
| `code.zip` regenerated from current `code/` | **PASS** | Regenerated in this audit session |
| `fix_output_consistency.py` excluded | **PASS** | Not included in zip (documented as non-pipeline helper) |
| Dataset excluded | **PASS** | No `dataset/` in zip |
| `.env` excluded | **PASS** | Not in zip |
| `__pycache__` / `.pytest_cache` excluded | **PASS** | Not in zip |
| Virtualenv excluded | **PASS** | Not in zip |
| Old/stale artifacts excluded | **PASS** | `code_dirty_old.zip`, locked output, empty `dataset/output.csv` removed |

**Note:** `code/src/fix_output_consistency.py` remains in the repo but is **not part of the normal pipeline**. It is a standalone helper for deterministic output patching when an output cannot be regenerated. It was intentionally excluded from `code.zip`.

---

## 7. Transcript Audit

| Check | Verdict | Notes |
|---|---|---|
| Logging exists | **PASS** | `%USERPROFILE%\hackerrank_orchestrate\log.txt` |
| Log generation works | **PASS** | Append-only, 511 lines at time of audit |
| Prompts/responses captured | **PASS** | Per-turn summary entries |
| Multi-agent workflow support | **PASS** | `parent_agent` field present |

**How to export/append all AI tool transcripts:**

1. Keep the shared log file at `%USERPROFILE%\hackerrank_orchestrate\log.txt`.
2. If you used multiple tools, copy each tool’s conversation export and paste it into the same file with a clear divider, e.g.:
   ```text
   ## [timestamp] TRANSCRIPT: ToolName
   ...
   ```
3. Before submission, redact any API keys, tokens, or PII.
4. Upload the single combined `log.txt` as the chat transcript.

---

## 8. Accuracy & Confidence

Metrics source: `code/evaluation/evaluation_report.md` (last successful live run).

| Metric | Accuracy |
|---|---|
| Exact row accuracy | **30.00%** |
| `claim_status` | **85.00%** |
| `object_part` | **80.00%** |
| `issue_type` | **55.00%** |
| `severity` | **50.00%** |
| `risk_flags` | **65.00%** |
| `evidence_standard_met` | **95.00%** |
| `supporting_image_ids` | **90.00%** |
| `valid_image` | **95.00%** |

The strongest dimension is `claim_status` (85%). The weakest are `severity` (50%) and `issue_type` (55%), which are the primary sources of exact-row errors.

---

## Top 10 Suspicious Test Predictions

See `submission_confidence/top_suspicious_predictions.csv`.

Key patterns flagged:
- `text_instruction_present` flagged but claim still `supported`.
- Package contents/item claims supported on exterior-only parts.
- `evidence_standard_met=false` rows with non-empty `supporting_image_ids`.
- High flag-count rows indicating model uncertainty.

**Caveat:** These are heuristic flags, not ground-truth errors.

---

## Submission Readiness Score

**78 / 100**

### Critical Blockers (all resolved)
1. ~~Stale `code.zip`~~ — regenerated from current `code/`.
2. ~~Stale root artifacts~~ — removed.

### Medium Risks
1. Live evaluation could not be refreshed; metrics are from the last successful run.
2. Sample exact-row accuracy is 30%.
3. `severity` calibration is weak (50%).
4. `evidence_requirements.csv` is used only as prompt text, not structurally.
5. `manual_review_required` appears on ~30/44 test rows.

### Low Risks
1. `code/src/fix_output_consistency.py` remains in repo but is excluded from zip and documented.
2. No root `.gitignore`.
3. Some contradicted rows include all image IDs due to the supporting-ID consistency filler.

### Recommended Fixes (ranked by scoring impact)
1. **Refresh live sample evaluation** once network/API access is available.
2. **Improve severity calibration** — target the 10 mismatched sample rows.
3. **Strengthen prompt-injection detection** — catch “follow the note” and “skip manual review” more reliably.
4. **Tighten package contents guardrail** — downgrade `supported` when claim is clearly about contents but only exterior packaging is visible.
5. **Reduce over-flagging of `manual_review_required`**.

---

## Final Verdict

**Submit after minor fixes — the minor fixes identified in this audit have been applied.**

`output.csv` is safe to submit: it is schema-valid, has 44 rows, and was produced by the latest prediction code. The only outstanding item is refreshing the live evaluation metrics, which requires a working API connection outside this environment.
